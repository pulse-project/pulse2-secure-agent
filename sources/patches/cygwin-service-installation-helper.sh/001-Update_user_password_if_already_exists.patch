diff --git a/binaries/data/cygwin/usr/share/csih/cygwin-service-installation-helper.sh b/binaries/data/cygwin/usr/share/csih/cygwin-service-installation-helper.sh
index 24a911b..9557eef 100755
--- a/binaries/data/cygwin/usr/share/csih/cygwin-service-installation-helper.sh
+++ b/binaries/data/cygwin/usr/share/csih/cygwin-service-installation-helper.sh
@@ -3014,6 +3014,14 @@ csih_create_privileged_user()
       # ${csih_PRIVILEGED_USERNAME} already exists. Use it, and make no changes.
       # use passed-in value as first guess
       csih_PRIVILEGED_PASSWORD="${password_value}"
+      # Update password to match current one
+      if net user "${username}" "${password_value}" >/tmp/siveo1.$$ 2>&1; then
+        csih_inform "${username}'s password has been updated to ${password_value}."
+      else
+        csih_warning "Unable to update ${username}'s password."
+       cat /tmp/siveo1.$$
+      fi
+      rm /tmp/siveo1.$$
       return 0
     fi
 
diff --git a/sources/patches/cygwin-service-installation-helper.sh/001-Update_user_password_if_already_exists.patch b/sources/patches/cygwin-service-installation-helper.sh/001-Update_user_password_if_already_exists.patch
index f9e8206..e69de29 100644
--- a/sources/patches/cygwin-service-installation-helper.sh/001-Update_user_password_if_already_exists.patch
+++ b/sources/patches/cygwin-service-installation-helper.sh/001-Update_user_password_if_already_exists.patch
@@ -1,17 +0,0 @@
---- cygwin-service-installation-helper.sh.orig	2009-03-03 09:15:29.797793915 +0100
-+++ cygwin-service-installation-helper.sh	2009-03-03 09:15:42.033846999 +0100
-@@ -2447,6 +2447,14 @@
-       # ${username} already exists. Use it, and make no changes.
-       # use passed-in value as first guess
-       csih_PRIVILEGED_PASSWORD="${password_value}"
-+      # Update password to match current one
-+      if net user "${username}" "${password_value}" >/tmp/mdv1.$$ 2>&1; then
-+        csih_inform "${username}'s password has been updated to ${password_value}."
-+      else
-+        csih_warning "Unable to update ${username}'s password."
-+	cat /tmp/mdv1.$$
-+      fi
-+      rm /tmp/mdv1.$$
-       return 0
-     fi
- 
